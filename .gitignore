# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# VSCode
.vscode/

# Node
node_modules/
dist/

# Environment
.env
.env.local

# Gradle build cache & outputs (절대 git에 올리면 안 됨)
.gradle/
build/
BackEnd/.gradle/
BackEnd/build/

# 로컬 전용 설정 파일 (DB 비밀번호 등 민감 정보 포함)
**/application-local.yml
**/application-secret.yml

# TLS 인증서/개인키 (Cloudflare Origin Certificate 등) — 절대 커밋 금지
certs/
*.pem
*.key