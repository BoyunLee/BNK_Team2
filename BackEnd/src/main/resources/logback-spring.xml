<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ───────── 공통 패턴 ───────── -->
    <!-- MDC: traceId, customerId, requestIp 자동 포함 -->
    <property name="LOG_PATTERN_CONSOLE"
              value="%d{HH:mm:ss.SSS} %highlight(%-5level) [%cyan(%X{traceId:--}) | cid=%yellow(%X{customerId:--}) | %X{requestIp:--}] %logger{36} - %msg%n"/>

    <property name="LOG_PATTERN_FILE"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:--}|cid=%X{customerId:--}|%X{requestIp:--}] %logger{50} - %msg%n"/>

    <property name="LOG_PATH" value="./logs"/>

    <!-- ───────── local 프로파일: 컬러 콘솔 출력 ───────── -->
    <springProfile name="local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${LOG_PATTERN_CONSOLE}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="com.busanbank.loan" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <!-- ───────── dev / prod 프로파일: 파일 출력 ───────── -->
    <springProfile name="dev,prod">

        <!-- 전체 애플리케이션 로그 -->
        <appender name="FILE_APP" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/application.log</file>
            <encoder>
                <pattern>${LOG_PATTERN_FILE}</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <timeBasedFileNamingAndTriggeringPolicy
                        class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                    <maxFileSize>100MB</maxFileSize>
                </timeBasedFileNamingAndTriggeringPolicy>
                <maxHistory>30</maxHistory>
                <totalSizeCap>3GB</totalSizeCap>
            </rollingPolicy>
        </appender>

        <!-- 거래 전용 로그: TX_ENTER / TX_EXIT / TX_BIZ_FAIL / TX_SYS_FAIL -->
        <appender name="FILE_TX" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/transaction.log</file>
            <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
                <evaluator class="ch.qos.logback.classic.boolex.OnMarkerEvaluator">
                    <marker>TX</marker>
                </evaluator>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
            <!-- TX 마커 없이 [TX_로 시작하는 메시지도 포함 -->
            <encoder>
                <pattern>${LOG_PATTERN_FILE}</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/transaction.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>90</maxHistory>
            </rollingPolicy>
        </appender>

        <!-- 에러 전용 로그 -->
        <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/error.log</file>
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>ERROR</level>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
            <encoder>
                <pattern>${LOG_PATTERN_FILE}</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>90</maxHistory>
            </rollingPolicy>
        </appender>

        <!-- 비동기 래퍼 (I/O 블로킹 최소화) -->
        <appender name="ASYNC_APP" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="FILE_APP"/>
            <queueSize>1024</queueSize>
            <discardingThreshold>0</discardingThreshold>
        </appender>

        <appender name="ASYNC_TX" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="FILE_TX"/>
            <queueSize>1024</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <includeCallerData>true</includeCallerData>
        </appender>

        <root level="INFO">
            <appender-ref ref="ASYNC_APP"/>
            <appender-ref ref="FILE_ERROR"/>
        </root>

        <!-- 거래 로그 Aspect는 별도 파일에도 기록 -->
        <logger name="com.busanbank.loan.global.logging.aspect.TransactionLoggingAspect"
                level="INFO" additivity="true">
            <appender-ref ref="ASYNC_TX"/>
        </logger>

        <!-- HTTP 필터 로그도 거래 파일에 포함 -->
        <logger name="com.busanbank.loan.global.logging.filter.MdcLoggingFilter"
                level="INFO" additivity="true">
            <appender-ref ref="ASYNC_TX"/>
        </logger>
    </springProfile>

</configuration>
