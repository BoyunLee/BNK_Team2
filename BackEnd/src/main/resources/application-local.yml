spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bnk3?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "!skdlxm12"

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: gusrb2776@gmail.com
    password: bflwqvzvumoxmwsh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

server:
  servlet:
    session:
      cookie:
        secure: false

# 로컬 개발용 AES-256 키 (32바이트 = 256비트, Base64 인코딩)
# 실제 운영 키는 절대 코드에 커밋하지 말 것 — Vault 또는 환경변수로 주입
crypto:
  secret-key: dGVzdGtleTF0ZXN0a2V5MnRlc3RrZXkzdGVzdGtleTQ=

# Claude API 키 — https://console.anthropic.com에서 발급
# 미설정 시 AI 요약 기능이 비활성화됩니다 (스케줄러/수동 트리거 모두)
anthropic:
  api-key: ${ANTHROPIC_API_KEY:}

logging:
  level:
    com.busanbank.loan: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
